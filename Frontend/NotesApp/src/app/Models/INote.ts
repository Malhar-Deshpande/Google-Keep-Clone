export interface INote {
    backgroundColor: string
    createdAt: Date
    isDeleted: boolean
    isPinned: boolean
    label: string
    noteContent: string
    noteId: number
    noteTitle: string
}